<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechConn"
        tools:targetApi="31">
        <activity
            android:name=".viewModel.InicioBarActivity"
            android:exported="false" />
        <!-- <activity
            android:name=".vista.InicioActivity"
            android:exported="false"
            android:label="@string/title_activity_inicio"
            android:theme="@style/Theme.TechConn.NoActionBar" />-->
        <activity
            android:name=".viewModel.LoginDimActivity"
            android:exported="false" />
        <activity
            android:name=".viewModel.LoginImdisActivity"
            android:exported="false" />
        <activity
            android:name=".viewModel.SelectorActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>